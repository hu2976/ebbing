# 截图产物。14M，而且是构建出来的——每次跑 shot.mjs 都会重生成。
# ⚠️ 它们在工程目录里，微信开发者工具的"代码质量"会把整个工程算进主包，
#    于是报"主包尺寸应小于 1.5M 未通过"。project.config.json 的
#    packOptions.ignore 已排除 .sync-mp，所以打包上传不会带上。
04-小程序/.sync-mp/out/

# 本机偏好，不是项目的一部分
project.private.config.json
.DS_Store
**/.DS_Store

# 依赖
node_modules/

# ⚠️ 云函数的密钥走云开发控制台的环境变量（OPENROUTER_KEY），
#    代码里从来没有它。这几条是防手滑。
.env
*.key
**/secrets.json

# 改前留的一手，不是项目的一部分
*.bak
.bak-*/

# 设计预览的截图产物，11M。hb.mjs / preview.mjs 跑一次就重生成，
# 和上面 .sync-mp/out 是同一类东西。
04-小程序/design-demos/hb-out/
04-小程序/design-demos/preview-out/
# card-gen.mjs 生成的 30 个 SVG，源是 card-gen.mjs 里的 SPEC
04-小程序/design-demos/cards/

# 设计探索的渲染截图。源就在旁边的同名 .html，跑一次就重出，没必要占 10M 仓库
04-小程序/design-demos/*.png
# 历次调试留下的临时探针（对比页、量尺寸的小页面），不是设计稿
04-小程序/design-demos/_*
