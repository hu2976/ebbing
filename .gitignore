# 截图产物。14M，而且是构建出来的——每次跑 shot.mjs 都会重生成。
# ⚠️ 2026-08-29 更正：packOptions.ignore 实测不生效（见 04-小程序/README.md）。
#    .sync-mp/ 没被打进预览包，靠的是"工具默认跳过 . 开头的目录"，不是 ignore。
#    大文件要么放 . 开头的目录里，要么放小程序根目录外——design-demos 走的是后者。
04-小程序/.sync-mp/out/

# 本机偏好，不是项目的一部分
project.private.config.json
.DS_Store
**/.DS_Store

# 依赖
node_modules/

# ⚠️ 云函数的密钥走云开发控制台的环境变量（OPENROUTER_KEY），
#    代码里从来没有它。这几条是防手滑。
.env
*.key
**/secrets.json

# 改前留的一手，不是项目的一部分
*.bak
.bak-*/

# 设计预览的截图产物，11M。hb.mjs / preview.mjs 跑一次就重生成，
# 和上面 .sync-mp/out 是同一类东西。
design-demos/hb-out/
design-demos/preview-out/
# card-gen.mjs 生成的 30 个 SVG，源是 card-gen.mjs 里的 SPEC
design-demos/cards/

# 设计探索的渲染截图。源就在旁边的同名 .html，跑一次就重出，没必要占 10M 仓库
design-demos/*.png
# 历次调试留下的临时探针（对比页、量尺寸的小页面），不是设计稿
design-demos/_*

# 路演的视频导出。单份 40M(mp4)+21M(webm)，00-交付-定版/03-路演 和 12-路演deck
# 各存了一份，共约 145M。git 存二进制不做增量——改一版就整份再存一遍，
# 仓库很快就失控且没法回收。视频走别处分发，PDF 留下（7.6M，能直接看）。
*.mp4
*.webm
# 录制脚本的工作目录：playwright 依赖 + 中间产物，跑一次就重生成
12-路演deck/.rec/

# 多端应用模式（Donut）的残留配置。2026-08-29 工具误切到那个模式时生成的，
# 它一在就会让工具重新进多端模式，而多端模式下 wx.cloud.init 拿不到当前环境
# （errCode -601002）。这个项目做的是小程序，不提交它。
project.miniapp.json
**/app.miniapp.json
