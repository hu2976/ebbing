# 给 Eazo 的 prompt（退潮）

---

做一个叫「退潮」的中文 app。深色，安静，像一本手账。

## 它是什么

给暴食、催吐这类进食困扰的人用的匿名自助工具。无账号、无登录、不联系人、不社交。
主线不是"记录暴食"，是**平时就在的陪伴**；难受那一刻的急救只是兜底入口，不是主角。

## 六条硬规则（最重要，请严格遵守）

1. **界面上一个临床词都不许出现**：症状、患者、病人、疾病、障碍、治疗、康复、诊断、进食障碍、暴食症——全部禁止。用户不是病人，界面不能预设她是谁。
2. **不显示任何数字化的身体指标**：热量、卡路里、克数、体重、BMI、营养成分、macros，一个都不要。
3. **不做打卡、连续天数、进度条、成就徽章、评分。** 没有"你已坚持 7 天"这类东西。
4. **不评价用户**。包括反向评价——"你已经很棒了""这不是你的错"这类也不要写。句子的主语尽量是食物和时间，不是"你"。
5. **不做 AI 聊天机器人、不做生成式对话安慰。** 所有文字是写死的。
6. **下面给出的所有中文文案请一字不改地原样使用**，不要润色、不要改写、不要"优化得更温暖"，也不要自己另外生成新句子。它们是逐句打磨过的。

## 视觉

- 背景 `#171310`，上面浮两团很淡的氛围光，一团暖一团冷。
- 强调色用**黄铜色**，冷侧用**青玉色**，都要低饱和。不要用亮蓝或纯白大色块。
- 手账质感：左侧留一条装订边、手绘感的横线（两端细中间粗）、关键词下面是手写下划线、箭头是手画的 `→`。
- 字号只用 5 档：12 / 15 / 18 / 25 / 29 px。间距只用 4 档：4 / 8 / 16 / 28 px。圆角只用两种：14px（卡片）和全圆（按钮）。
- 每一页都要在一屏内装下，不滚动。

## 页面

### 首页
从上到下：

- 小标题「今天的滋味」，下面一张**米白色的纸质卡片**，浮在深色背景上，有影子，轻微旋转 0.5 度，卡上写一样食物的名字（比如"烤红薯"）。点开是滋味卡详情页。
- 一条手绘横线。
- 小标题「今天怎么样」，下面三个空格子（代表三顿饭，点一下填上，不显示任何数字）。
- 格子下面一行小字，原样：`三顿正餐 + 三次加餐，中间隔三到四个小时。隔太久，后面会难扛一些。`
- 三个次级入口，文字原样：`情绪瓶子` / `昨晚睡得怎么样` / `找个人说说话`
- 一条手绘横线。
- 一个小方块，原样：`20:30 · 十五分钟 · 一起坐着`，下面一行原样：`此刻 23 人在`，再下面一个入口原样：`那场是什么`
- 一个大按钮，原样：`我现在不太好`
- 一个次级入口，原样：`试试含一口水，慢慢感受`
- 页脚两条细链接，原样：`身体不舒服` / `什么时候必须去医院`

⚠️ 首页不要出现"你好""欢迎""你来了"这类问候语，也不要问用户今天有没有暴食。

### 点「我现在不太好」进入的页
标题原样：`你来了。`
三个按钮，原样：`很想吃` / `正在吃` / `吃完了`
一个次级入口，原样：`吃不下 · 说不上来`

### 「很想吃」
标题原样：`你来了。什么都先放着。`（两行）
两个按钮，原样：`今天吃得很少` / `今天吃过了`
次级入口，原样：`现在什么都不想回答`

### 「今天吃得很少」
标题原样：`饿了。`
一句原样：`太久没吃东西，饿的感觉会像潮水一样涨起来。`
下面一个缓慢起伏的**潮水动画**（一条横向的波纹，很慢，不循环得太明显）。
一条横线，然后小标题原样：`想吃点什么样的？`
五个可选项，原样：`热乎乎的` / `冰冰凉的` / `软的、好咽的` / `有嚼头的` / `说不上来`

选完之后给对应的三句话，原样使用，一个数字都不要加：

- 热乎乎的 → `一碗热汤面，卧个蛋。` / `或者热汤泡饭，加一块肉。` / `汤面里放点青菜，油照常放。`
- 冰冰凉的 → `牛奶泡麦片，加个香蕉。` / `或者酸奶加一把坚果。` / `凉皮也行，加个蛋。`
- 软的、好咽的 → `粥，加一个蛋。` / `或者蒸蛋配米饭。` / `软面条卧蛋也一样。`
- 有嚼头的 → `一碗拌面，筋道的那种。` / `或者米饭配炒菜，菜里有肉。` / `花生米、黄瓜段，能咬得响的都算。`
- 说不上来 → `米饭、面条、馒头，哪样都行。` / `配一个蛋，或者一块肉。` / `有菜就加一样，没有也行。`

### 「今天吃过了」
标题原样：`它会自己退。等着就行。`（两行）
⚠️ **这一页绝对不要放倒计时或计时器。**
一个按钮原样：`把手放到冷水下面`
一行原样：`含一块冰` —— 副文案原样：`慢慢感受口腔冰冰凉凉的感觉`
一个方块，原样：`现在想去抵消掉刚才那顿的话，先别。那条路事后更难。`
一个按钮原样：`用了，这阵过去了`

### 「正在吃」
标题原样：`吃着就行。一次不会定下什么。`（两行）
一句原样：`开着灯，用一只碗。包装可以先扔。`
一句原样：`这一页就停在这儿。`
方块原样：`今天之后可能会觉得肿，秤上也会重一点。那是水，几天就下去了。`
一行原样：`现在这一页上，还有 12 个人。`
小字原样：`谁也不知道谁是谁。`
两个入口：`身体不太对`（红色）/ `吃完了`

### 「吃完了」
标题原样：`结束了。`
方块原样：`现在想去抵消掉刚才那顿的话，先别。那条路事后更难。`
一句原样：`先看看身体。`
两个按钮：`有点不舒服`（红色）/ `还行`

### 「还行」之后
标题原样：`过去了。`
一句原样：`吃多了一点，或者少了一点，都没有把今天定下来。`
方块原样：`那是一阵，不是你。`
一个入口原样：`试试含一口水，慢慢感受`

### 「有点不舒服」
标题原样：`嘴里和喉咙可能会不舒服。`（两行）
四行提示，每行是「一句 + 一句解释」，原样：

- `先别刷牙` —— `这时候刷会磨掉牙釉质。用清水或含氟漱口水漱一下，半小时后再刷，顺着牙横向轻刷`
- `水一小口一小口` —— `胃已经不舒服了，猛灌会更胀`
- `别平躺` —— `垫高一点`
- `下一顿还是到点吃` —— `相信我们的身体`

一条横线，小标题原样：`下一顿吃点什么`
`温的、软的，好咽的。`
`粥或者软面条，卧一个蛋。或者牛奶泡面包。`
小字原样：`这几天先避开很酸、很辣、很硬、很烫的——刚才那一下之后，喉咙和食管都比平时脆。`
小字原样：`一次少一点，隔一会儿再吃一点，比一大顿好受。`
方块原样：`饭就是饭。吃下去，人就有力气。`

### 「吃不下 · 说不上来」
标题原样：`好。`
一句原样：`愿意做点其他的什么吗。`
一个按钮原样：`一起正念`
一个入口原样：`试试含一口水，慢慢感受`

## 最后

每一页左上角有一个返回，文字就是 `← 返回`，位置在所有页面都固定不变。
所有数据只存在本机，不上传。
